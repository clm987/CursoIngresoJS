function mostrar()
{

    var clave = prompt("ingrese el número clave.");

	while(clave != 'utn750')
		{
           clave=prompt("Clave inválida reingrese la clave "  )
            
		}
   
        alert("Clave válida")

}//FIN DE LA FUNCIÓN
